import SwiftUI

@main
struct yidiantongApp: App {
    // 这里可以添加数据模型的初始化
    
    var body: some Scene {
        WindowGroup {
            ContentView()
                .preferredColorScheme(.light)
        }
    }
}
